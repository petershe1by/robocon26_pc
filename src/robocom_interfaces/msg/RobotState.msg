# 机器人状态
float64 x              # 当前x坐标 (mm)
float64 y              # 当前y坐标 (mm)
float64 yaw            # 当前朝向角度 (rad)
float64 velocity       # 当前速度 (m/s)
bool armed             # 使能状态
bool on_ground         # 是否在地面上
builtin_interfaces/Duration mission_time  # 比赛用时
